    var isframe = window.top != window.self;    var boxtitle = isframe ? '' : '创建新帐�?';    function refresh(){        isframe ? $(document).trigger('reload') : location.reload();    }    function loadBackForm(API){        isframe ? $('.syn_wrap').load(API) : ui.box.load( API, {title: boxtitle});    }    var loadWrap = isframe ? $('.syn_wrap') : ui.box;    function bindSinaCallBack(result,username,img) {        if (sinaOpener != null) sinaOpener.close();        if (result == 1) {        	if(username == '1'){        		refresh();            }else{            ui.box.close();	            loadBackForm(U('home/Public/other_login'));            $('.otherclass').val("sina");            $('#otherimg').attr("src",img);            $('#othername').text(username);            $('#other_uname').val(username);            $('#othersign').text("新浪");}        }else {        	        /*var msg = this.msg(result,'sina');        setting.showMsg(msg,180,2);*/        }        }        function bindQzoneCallBack(result,username,img) {        if (qzoneOpener != null) qzoneOpener.close();        if (result == 1) {        	if(username == '1'){        		refresh();            }else{            	ui.box.close();	            loadBackForm(U('home/Public/other_login'));            //ui.box.show($('#tsbox').html(), {title:'',closeable:true});            $('.otherclass').val("qzone");            $('#otherimg').attr("src",img);            $('#othername').text(username);            $('#other_uname').val(username);            $('#othersign').text("QQ空间");}        	        }else {            /*var msg = this.msg(result,'sina');             setting.showMsg(msg,180,2);*/        }    }        function bindQQCallBack(result,username,img) {        if (qqOpener != null) qqOpener.close();        if (result == 1) {        	if(username == '1'){        		refresh();            }else{            loadBackForm(U('home/Public/other_login'));            $('.otherclass').val("qq");            $('#otherimg').attr("src",img);            $('#othername').text(username);            $('#other_uname').val(username);            $('#othersign').text("腾讯微博");}        }else {            /*var msg = this.msg(result,'sina');             setting.showMsg(msg,180,2);*/        }    }        function bindDOUBANCallBack(result,username,img) {        if (doubanOpener != null) doubanOpener.close();        if (result == 1) {        	if(username == '1'){        		refresh();            }else{            ui.box.close();	            loadBackForm(U('home/Public/other_login'));            $('.otherclass').val("douban");            $('#otherimg').attr("src",img);            $('#othername').text(username);            $('#other_uname').val(username);            $('#othersign').text("豆瓣");}        }else {            /*var msg = this.msg(result,'sina');             setting.showMsg(msg,180,2);*/        }    }        function bindRENRENCallBack(result,username,img){    	if (renrenOpener != null) renrenOpener.close();        if (result == 1) {        	if(username == '1'){        		refresh();            }else{            ui.box.close();	            loadBackForm(U('home/Public/other_login'));            $('.otherclass').val("renren");            $('#otherimg').attr("src",img);            $('#othername').text(username);            $('#other_uname').val(username);            $('#othersign').text("人人");}        }else {            /*var msg = this.msg(result,'sina');             setting.showMsg(msg,180,2);*/        }    }        function bindTAOBAOCallBack(result,username,img){    	if (taobaoOpener != null) taobaoOpener.close();        if (result == 1) {        	if(username == '1'){        		refresh();            }else{            ui.box.close();	            loadBackForm(U('home/Public/other_login'));            $('.otherclass').val("renren");            $('#otherimg').attr("src",img);            $('#othername').text(username);            $('#other_uname').val(username);            $('#othersign').text("淘宝");}        }else {            /*var msg = this.msg(result,'sina');             setting.showMsg(msg,180,2);*/        }    }	function changeverify() {    var date = new Date();    var ttime = date.getTime();    var url = U('home/Public/verify');    $('#verifyimg').attr('src',url+'&'+ttime);    }